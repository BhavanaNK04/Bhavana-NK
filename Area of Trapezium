#include <stdio.h>

int main() {

    double b1, b2, h;
    if (scanf("%lf %lf %lf", &b1, &b2, &h) != 3) {
        return 1; // Input error
    }

    double area = 0.5 * (b1 + b2) * h;

    printf("%.4f\n", area);

    return 0;
}
