#include <stdio.h>

int main() {

    double X, Y;

    if (scanf("%lf %lf", &X, &Y) != 2) {
        return 1; // Input error
    }

    double time_taken=(X*Y)/(X+Y);
    
    int result = (int)time_taken;

    // Output the time taken
    printf("%d\n", result);

    return 0;
}
